[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "deltax"
version = "0.1.0"
description = "Prematch odds drop monitors (Tipsport + Pinnacle) with Telegram alerts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cloudscraper25>=2.7.0",
    "curl_cffi>=0.7.0",
    "fake-useragent>=2.0.0",
    "httpx>=0.28.0",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0.0",
    "PyYAML>=6.0",
    "user-agents>=2.2.0",
]

[project.scripts]
deltax-monitor-tipsport = "deltax.monitor:main"
deltax-monitor-pinnacle = "deltax.monitor:main"
deltax-settle-tipsport = "deltax.settle.worker:main"
# Deprecated console script names (Tipsport monitor / settler)
deltax-monitor = "deltax.monitor:main"
deltax-settle = "deltax.settle.worker:main"

[tool.setuptools.packages.find]
where = ["src"]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
